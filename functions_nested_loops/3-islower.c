#include "main.h"

/**
 *islower- check if carac is lower or not
 *
 *Return: always
 */

/**  if lower case return 1 else return 0
 */

int _islower(int c)

{

  if (c > 96 && c < 123)
{
  return (1);
    }
  else
{
  return (0);
    }
}
