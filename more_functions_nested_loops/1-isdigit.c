#include "main.h"

/**
 *_isdigit- it's a digit so type 1 else type 0
 *@c: euh yes
 *description: oui
 *Return: 1 if digit
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
