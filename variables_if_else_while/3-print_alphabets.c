#include <stdio.h>

/**
 *main- double alphabet
 *Return: always (success)
 */

int main(void)
{
char c;
for (c = 'a' ; c <= 'z'; c++)
{
putchar(c);
}
char d
for (d = 'A' ; d <= 'Z'; c++)
{
putchar(d);
}
putchar('\n');
return (0);
}
